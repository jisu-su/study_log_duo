> **프로젝트명:** studuo
**서비스명:** duoingsu
**작성/갱신일:** 2026년 5월 24일
**대상 사용자:** 고정 2인
**목표:** 두 사람이 하루 플랜, 시간별 실행 로그, 회고, 자료, 응원, 알림을 함께 쌓아가는 2인 전용 스터디 기록 웹앱
**타겟 환경:** 데스크탑/노트북 웹 브라우저 기준, 모바일은 Push 알림 수신용 PWA 사용
> 

---

## 변경 이력

| 버전 | 변경 내용 |
| --- | --- |
| v1 | 최초 작성 |
| v2 | 모바일 대응 제외 / 30초 폴링 확정 / @hono/firebase-auth 인증 확정 |
| v3 | `logical_date` 도입 / 휴무 기능 / 일정·약속 기능 / 알림 3단계 필터 / DB 스키마 전면 업데이트 |
| v4 | 프로젝트명·서비스명 확정 / 닉네임 1회 설정 / 홈·플랜 생선가시 UI 통일 / 대시보드 분리 / 플랜 응원 / 다중 공감 / PWA Web Push / 플랜 중심 Cron 알림 정책 확정 |

---

## 1. 프로젝트 개요

### 1.1 배경

혼자 공부 기록을 남기는 것보다, 고정된 한 명과 서로의 계획과 실행을 공유할 때 지속성과 긴장감이 높아진다. **duoingsu**는 셋로그처럼 서로를 자연스럽게 재촉하되, 영상 대신 텍스트 기반의 계획·실행·회고 기록을 쌓는다.

### 1.2 핵심 컨셉

- 하루 시작 시 플랜과 하루 상태를 작성한다.
- 시간대별 계획을 세우고, 홈 타임라인에 실제 실행 로그를 남긴다.
- 상대방의 플랜·로그·회고를 볼 수 있다.
- 친구가 내 하루 상태에 응원글을 남길 수 있다.
- 친구가 내 회고에 여러 공감 이모지를 남길 수 있다.
- 기록이 없거나 계획 시간이 됐는데 로그가 없으면 Push 알림으로 알려준다.
- **"잠들기 전까지는 같은 날"** 이라는 원칙으로 `logical_date`를 사용한다.

### 1.3 기준 사용자

| 항목 | 내용 |
| --- | --- |
| 사용자 수 | 고정 2명 (회원가입·초대 기능 없음) |
| 가입 방식 | 별도 회원가입/초대 없음 |
| 인증 | Google 로그인 + 서버 화이트리스트 (허용 이메일 2개 고정) |
| 사용자 구분 | Firebase UID  |
| 표시 이름 | 최초 로그인 시 닉네임 1회 설정 (한글/영문/숫자, 2~12자) |
| 닉네임 변경 | 변경 불가 |
| 로그인 유지 | 기기당 최초 1회 로그인 후 영구 유지 (브라우저 데이터 삭제 전까지) |
| 플랫폼 | 웹 + PWA (알림 수신용) |

---

## 2. 하루 경계 설계

### 3.1 `logical_date`

`logical_date`는 실제 캘린더 날짜가 아니라 앱에서 사용하는 체감 날짜다.

기본 하루 시작 시각은 “**06:00”**이다.

```
실제 시각                logical_date (저장되는 날짜)
-----------------------------------------------------
4월 19일 07:00  →  4월 19일  ✅ 새 하루
4월 19일 23:00  →  4월 19일  ✅ 같은 날
4월 20일 01:30  →  4월 19일  ✅ 연장선 (아직 안 잠)
4월 20일 05:59  →  4월 19일  ✅ 연장선
4월 20일 06:00  →  4월 20일  ✅ 새 하루 시작
```

### 3.2 타임라인 순서

같은 `logical_date`의 시간축은 “**06:00 ~ 다음날 05:00”** 순서로 표시한다.

```
[4월 19일 타임라인]

06:00  알고리즘 풀기
07:00  ...
...
23:00  CS 복습
── 자정 ──────────────────────────
00:00  마무리 정리     (실제 4/20이지만 logical_date = 4/19)
01:00  (빈칸)
...
05:00  (빈칸)
```

### 3.3 운영 정책

`day_start_hour`는 기본 6으로 유지한다. 운영 중 변경 시 기존 데이터의 날짜 해석이 달라질 수 있으므로 변경하지 않는 것을 권장한다.

---

## 4. 메뉴 구성

```
├── 🏠 홈 (타임라인 피드)
├── 📊 대시보드 (주간 통계 & 계획과 실행도)
├── 📅 플랜 (내 하루 상태 & 플랜)
├── 💬 느낀 점 (감정 & 회고)
├── 📚 자료 공유
└── ⚙️  설정 (알림·휴무·일정 관리)
```

---

## 5. 기능 명세

### 5.1 공개/로그인 화면

로그아웃 또는 미인증 상태의 사용자는 로그인 화면만 볼 수 있다.

- 서비스명 표시
- 비공개 스터디 로그 안내
- Google 로그인 버튼
- 허용되지 않은 계정은 앱 내부 데이터 접근 불가

---

### 5.2 인증 및 닉네임

- Firebase Google 로그인 사용
- Worker에서 `@hono/firebase-auth`로 ID Token 검증
- `ALLOWED_EMAILS` 환경변수로 허용 계정 제한
- 최초 로그인 후 닉네임 설정 모달 표시
- 닉네임은 한 번 설정하면 앱 화면에서 변경 불가
- 닉네임은 앱 상단, 타임라인, 플랜, 회고 등에 표시

---

### 5.3 🏠 홈

**목적:** 오늘 둘이 각각 어떤 시간에 무엇을 했는지 한눈에 확인

**레이아웃 — 생선가시(Fish-bone) 2열 타임라인**

시간축이 화면 정중앙에 세로로 고정되고, 양쪽으로 두 사람의 로그가 나란히 표시된다.

- 왼쪽: 첫 번째 로그인 사용자 (오른쪽 정렬, 시간축으로 연결선)
- 가운데: 시간축 (06:00 ~ 다음날 05:00)
- 오른쪽: 두 번째 로그인 사용자 (왼쪽 정렬, 시간축으로 연결선)

**시간 셀 상태 4종**

| 상태 | 표시 방식 | 알림 발송 |
| --- | --- | --- |
| 로그 있음 | 작업 내용 + 태그 + 집중도 | 없음 |
| 로그 없음 | 빈 셀 (시각적 압박) | 발송 |
| 🏖️ 휴무 | "휴무" 배지, 회색 처리, 입력 차단 | 없음 |
| 📅 약속 | 약속 이름 + 파란 배지, 입력 차단 | 없음 |

**기능**

- 내 칸만 입력 가능, 상대 칸은 읽기 전용
- 빈 시간 셀 클릭 → 로그 입력 모달
- 날짜 변경 가능 (달력 팝업, 과거 기록 조회)
- 30초 폴링으로 최신 데이터 자동 반영

**로그 입력 필드**

- 작업 요약 (텍스트, 최대 200자)
- 태그 선택: 학습, 코딩, 회의, 휴식, 기타
- 집중도 선택: 낮음 / 보통 / 높음

---

### 5.4 📅 플랜

**목적:** 하루의 상태와 시간별 계획을 공유

**구성:** 

- 내 하루 상태
- 상대 하루 상태
- 상대 하루 상태에 대한 응원 메시지
- 시간별 계획 생선가시 UI

**오늘의 상태**

각자 자신의 하루 상태를 입력한다.

| 항목 | 입력 방식 |
| --- | --- |
| 컨디션 | 이모지 버튼 5단계 (😴 😞 😐 😊 🔥) |
| 날씨 | 아이콘 선택 (☀️ ⛅ 🌧️ ❄️ 🌫️) |
| 오늘 목표 한 줄 | 텍스트 입력 (50자 이내) |

**응원 기능**

상대방의 하루 상태 아래에 한 줄 응원 메시지를 작성할 수 있다.
응원 작성/수정 시 상대방에게 Push 알림이 발송된다.

**시간별 계획 — 생선가시 UI**

홈 타임라인과 동일한 생선가시 레이아웃으로 시간별 계획을 표시한다.

- 내 칸만 입력/수정 가능
- 휴무·약속 시간대에는 입력 불가
- 상대 계획은 읽기 전용
- 첫 로그인 사용자 왼쪽, 두 번째 로그인 사용자 오른쪽

---

### 5.5 💬 느낀 점

**목적:** 하루를 마무리하며 감정과 회고를 남기고, 상대방 회고에 공감한다.

**입력 항목**

- 오늘의 감정 태그 다중 선택: 집중됨 / 뿌듯함 / 산만함 / 지침 / 답답함 / 설렘 / 평온함 / 불안함
- 오늘 잘한 것
- 오늘 아쉬운 것
- 자유 회고

**상대방 회고 카드**

- 하단에 상대방 작성 내용 읽기 전용 표시
- 공감 이모지 다중 선택 가능: 👍 💪 ❤️
- 같은 이모지를 다시 누르면 취소
- 새 공감 추가 시 회고 작성자에게 Push 알림 발송
- 공감 취소 시에는 알림 미발송

---

### 5.6 📚 자료 공유

**목적:** 공부하다 발견한 링크, 파일, 메모를 빠르게 공유

**업로드 타입**

| 타입 | 내용 |
| --- | --- |
| 🔗 링크 | URL + 제목 + 메모  + OG 정보 |
| 📄 파일 | PDF, 이미지 (Cloudflare R2 저장, 최대 10MB) |
| 📝 메모 | 짧은 텍스트, 코드 조각 |

**기능**

- 검색 (제목, 태그 기준)
- 날짜 필터
- 태그
- 올린 사람 표시
- 핀 고정
- URL 중복 체크
- 파일 다운로드는 인증된 API 경유

---

### 5.7 📊 대시보드

**목적:** 계획과 실행의 차이를 확인하고, 주간 기록 흐름을 요약

**구성**

- 주간 기록률
- 평균 집중도
- 많이 쓴 태그
- 계획(plan_items)과 실행(time_logs) 비교표

**비교 기준:** 

- `plan_items`: 계획
- `time_logs` : 실행
- `day_offs`, `schedules` : 기록 대상 시간에서 제외

---

### 5.8 ⚙️ 설정

**구성:**

- 휴무 관리
- 약속 관리
- Push 알림 구독

**알림 설정**

- 기기별로 ‘이 기기에서 푸시 알림 받기’ 버튼 클릭 필요
- 구독 정보는 `push_subscriptions` 테이블에 저장

**휴무 관리**

- 달력에서 날짜 선택 → 휴무 등록/해제
- 휴무 메모 입력 가능 (예: "여행", "병원")

**일정·약속 관리**

- 날짜 + 시간 범위 + 약속 이름 입력
- 등록된 약속 목록 확인·삭제

---

## 6. 알림 정책

알림은 **Web Push**만 사용한다.

알림 문구는 Worker의 `PUSH_MESSAGES` 상수에서 관리한다.

<aside>

apps/worker/src/index.ts

</aside>

### 6.1 이벤트 알림 (사용자 행동 직후 발송)

| 조건 | 알림 대상 | 이동 위치 |
| --- | --- | --- |
| 친구가 오늘 하루 상태를 처음 저장 | 상대방 | 플랜 |
| 친구가 내 하루 상태에 응원글 작성/수정 | 나 | 플랜 |
| 친구가 내 회고에 공감 버튼 클릭 | 나 | 느낀 점 |

**정책 세부사항**

- 하루 상태 저장 알림은 같은 날짜 **첫 저장 시에만** 발송, 수정 시 미발송
- 응원글은 작성/수정 모두 발송
- 공감은 새로 누를 때만 발송, 취소 시 미발송
- 자기 자신에게는 알림 발송하지 않음

### 6.2 시간 기반 알림 (Cron — 매 정시 + 매 정시 10분)

```toml
crons = ["0 * * * *", "10 * * * *"]
```

### 6.2.1 당일 플랜 없음 알림

조건 (모두 충족 시 발송):

- 낮 12시 이후
- 오늘 휴무가 아님
- 오늘 약속이 없음
- 당일 `plan_items`가 하나도 없음

발송 방식: 12:00 첫 발송, 이후 플랜이 생길 때까지 매 정각 반복

### 6.2.2 플랜 시간 시작 알림

조건:

- 해당 시간에 `plan_items` 있음
- 오늘 휴무가 아님
- 해당 시간이 약속 시간대가 아님

발송: 계획된 시간 정각

예시: 16:00 플랜 있음 → 16:00에 알림

### 6.2.3 계획 대비 실행 기록 누락 알림

조건:

- 대상 시간에 `plan_items` 있음
- 대상 시간의 `time_logs` 없음
- 오늘 휴무가 아님
- 대상 시간이 약속 시간대가 아님

발송: 계획 시간으로부터 1시간 10분 뒤

예시: 16:00 플랜 있음, 17:10까지 16:00 로그 없음 → 17:10에 알림

### 6.3 조용한 시간대

**‘02:00 ~ 08:59’**에는 Cron 기반 알림을 보내지 않는다.

### 6.4 PWA 알림 수신 조건

### 안드로이드

- Chrome으로 접속
- 사이트 알림 권한 허용
- 앱 설정 페이지에서 Push 구독 버튼 클릭

### iOS / 아이패드

- **반드시 Safari**로 접속 (Chrome 앱 불가)
- 하단 공유버튼(□↑) → "홈 화면에 추가"
- 홈 화면 아이콘으로 실행
- 앱 설정 페이지에서 Push 구독 버튼 클릭

iOS 필수 조건: iOS 16.4 이상 + Safari + 홈화면 추가 완료 

---

## 7. 실시간 동기화 (30초 폴링)

WebSocket 대신 **30초 폴링**을 사용한다.

2인 앱이므로 하루 8시간 기준 약 960회 요청 → 무료 티어 한도 내 충분.

---

## 8. 기술 스택

### 8.1 전체 구성

| 영역 | 기술 |
| --- | --- |
| 프론트엔드 | React + Vite + TypeScript |
| API 서버 | Cloudflare Workers + Hono |
| 인증 | Firebase Auth + @hono/firebase-auth |
| DB | Cloudflare D1 |
| 파일 저장 | Cloudflare R2 |
| 알림 | Web Push + Service Worker + VAPID |
| 호스팅 | Cloudflare Pages |
| Push KV | Cloudflare KV |

### 8.2 스택 선택 이유

| 기술 | 선택 이유 |
| --- | --- |
| Cloudflare Pages | GitHub 연동 자동 배포, 무료, CDN 포함 |
| Cloudflare Workers + Hono | 서버리스 API, Express와 유사한 구조, Workers 최적화 |
| @hono/firebase-auth | Workers에서 Firebase 토큰 검증 호환 문제 없이 처리 |
| Cloudflare D1 | SQLite 기반, 2인 앱에 충분, 무료 (500MB / 500만 쿼리/월) |
| Cloudflare R2 | 이그레스 비용 없음, 무료 (10GB/월) |
| Firebase Auth | Google 로그인 무료, 토큰 자동 갱신, 기기당 영구 로그인 |
| 30초 폴링 | WebSocket 대비 구현 단순, 2인 앱에서 딜레이 체감 없음 |
| Web Push (PWA) | 네이티브 앱 없이 모바일 푸시 알림 가능 |

### 8.3 배포 리소스

| 항목 | 값 |
| --- | --- |
| Pages URL | https://duoingsu.pages.dev |
| Worker name | duoingsu |
| Firebase project id | studuo-fa42b |
| D1 database | duoingsu-db |
| R2 bucket | studuo-resources |
| KV binding | PUBLIC_JWK_CACHE_KV |

### 8.4 무료 티어 운영 가능 여부

| 서비스 | 무료 한도 | 예상 사용량 |
| --- | --- | --- |
| D1 | 쿼리 500만/월 | ~1만 |
| R2 | 10GB/월 | ~수백MB |
| Workers | 요청 10만/일 | ~수백 |
| Firebase Auth | MAU 10만 | 2명 |
| 예상 월 비용 |  | $0 |

---

## 9. 인증 설계

### 9.1 사용자 경험

첫 접속 (기기당 딱 한 번)

1. "Google로 로그인" 클릭
2. 구글 계정 선택
3. 닉네임 설정 (최초 1회)
4. 홈으로 이동

이후 같은 기기에서 재접속 시 로그인 화면 없이 바로 홈으로 이동한다.
브라우저 데이터를 삭제하거나 로그아웃하기 전까지 영구 유지된다.

### 9.2 서버 검증 흐름

1. 브라우저에서 Google 로그인 → Firebase ID Token 발급
2. API 요청 시 헤더에 토큰 첨부: Authorization: Bearer (token)
3. @hono/firebase-auth 미들웨어가 Google 공개키로 JWT 서명 검증
4. 이메일 추출 → ALLOWED_EMAILS 대조
5. 허용 → 요청 처리 / 거부 → 401 Unauthorized

### 9.3 보안 주의사항

| 항목 | 처리 방법 |
| --- | --- |
| 허용 이메일 목록 | Cloudflare Workers Secrets에 저장, 코드 하드코딩 금지 |
| Firebase Project ID | 동일하게 환경변수 처리 |
| VAPID Private Key | Worker Secret으로 관리 |
| R2 파일 | 버킷 private 설정, 인증된 Workers API 경유로만 다운로드 |

---

## 10. DB 스키마

| 테이블 | 역할 |
| --- | --- |
| users | 사용자, 닉네임, 하루 시작 기준 |
| plans | 일별 하루 상태 (컨디션, 날씨, 목표) |
| plan_items | 시간별 계획 (생선가시 입력) |
| time_logs | 시간별 실행 로그 |
| day_offs | 휴무 |
| schedules | 약속·일정 |
| reflections | 회고 |
| reactions | 회고 공감 |
| resources | 자료 공유 |
| plan_cheers | 하루 상태 응원 메시지 |
| push_subscriptions | 기기별 Push 구독 정보 |

마이그레이션 파일 목록:

- 0001_init.sql
- 0002_users_name_locked.sql
- 0003_reactions_multi_emoji.sql
- 0004_plan_cheers.sql
- 0005_push_subscriptions.sql

---

## 11. 개발 단계 (마일스톤)

### Phase 1 — 뼈대 ✅ 완료

- Cloudflare Pages + Workers + D1 초기 세팅
- Hono + @hono/firebase-auth 미들웨어 설정
- Firebase Auth Google 로그인 + 화이트리스트 검증
- 닉네임 1회 설정
- logical_date 계산 함수 구현
- 기본 레이아웃 (네비게이션)
- 홈 타임라인 기본 CRUD + 30초 폴링

### Phase 2 — 핵심 기능 ✅ 완료

- 플랜 페이지 생선가시 레이아웃
- 휴무 등록/해제
- 일정·약속 등록/삭제
- 타임라인 셀 상태 4종 표현
- 느낀 점 페이지 (감정 태그, 회고, 다중 공감)
- 자료 공유 (링크 OG 파싱, R2 파일 업로드, 메모)
- 대시보드 (계획 vs 실행 비교)
- 날짜 이동 및 과거 기록 조회

### Phase 3 — 알림 & 마무리 ✅ 완료

- PWA 설정 (manifest.json, 서비스 워커)
- Web Push 구독 + VAPID
- 이벤트 알림 (응원, 공감)
- 플랜 중심 Cron 알림 (플랜 없음 / 시작 / 누락)
- iOS 홈화면 추가 안내 배너
- 설정 페이지

### Phase 4 — 개선 후보 (미구현)

| 항목 | 우선순위 | 내용 |
| --- | --- | --- |
| 알림 설정 세분화 | 중 | 사용자별 알림 ON/OFF, 시간대 설정 |
| 온보딩 | 중 | 첫 사용 안내 |
| 통계 고도화 | 낮음 | 월간/태그/스트릭/히트맵 |
| 모바일 UI 최적화 | 낮음 | 현재는 알림 수신 중심 PWA |
| 다크모드 | 낮음 | 선택 기능 |

---

## 12. 리스크 및 대응

| 리스크 | 내용 | 대응 |
| --- | --- | --- |
| iOS Push 조건 | Safari + 홈화면 추가 + 권한 허용 3조건 동시 충족 필요 | 인앱 안내 배너 + 실제 기기 테스트 |
| Android Push 누락 | 사이트 권한 또는 구독 버튼 미클릭 | 설정 페이지 구독 버튼 안내 |
| logical_date 변경 | 기존 데이터 날짜 해석이 달라질 수 있음 | 운영 중 day_start_hour 변경 지양 |
| 알림 과다 | 플랜·실행 알림이 잦을 수 있음 | 추후 사용자별 알림 설정 추가 |
| R2 파일 접근 | URL 직접 노출 시 외부 접근 가능 | 인증된 Worker API로만 다운로드 |

---

## 13. 배포 전 체크리스트

**타입체크 및 빌드**

- npm -w apps/web run typecheck
- npm -w apps/worker run typecheck
- npm -w apps/web run build

**환경변수 확인**

- Worker 환경변수/시크릿 확인
- Pages 환경변수 확인
- D1 마이그레이션 적용 여부 확인

---

*v4 — 2026년 5월 24일 기준 최종 PRD*